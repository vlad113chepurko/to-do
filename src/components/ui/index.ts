import { Button } from "./button";
import { Spinner } from "./spinner";
import { Input } from "./input";
import { Label } from "./label";

export const ui = {
  Button,
  Spinner,
  Input,
  Label,
};
